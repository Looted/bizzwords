import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bizzwords',
  appName: 'BizzWords',
  webDir: 'dist/bizzwords/browser'
};

export default config;
